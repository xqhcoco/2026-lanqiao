#include<iostream>
#include<string>
using namespace std;
int main()
{
	int t;
	cin >> t;
	while (t--) {
		string s;
		cin >> s;
		int cntA = 0;
		for (auto& c : s) {
			if (c == 'A') cntA++;
		}
		if (cntA > 2)cout << 'A' << endl;
		else cout << 'B' << endl;
	}
	return 0;
}
